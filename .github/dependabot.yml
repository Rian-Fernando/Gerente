version: 2
updates:
  - package-ecosystem: npm
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      # react and react-dom must move together — React refuses to boot if the
      # two versions differ, so separate PRs for each are unmergeable by
      # construction and always fail CI.
      react:
        patterns:
          - "react"
          - "react-dom"
      dev-dependencies:
        dependency-type: development
    labels:
      - dependencies
    ignore:
      # These majors are deliberate migrations, not automated bumps:
      #  - React 19 needs react + react-dom upgraded together, plus testing.
      #  - three ships breaking renderer/addon changes on minor releases, and the
      #    landing scene is hand-tuned against the pinned version.
      - dependency-name: "react"
        update-types: ["version-update:semver-major"]
      - dependency-name: "react-dom"
        update-types: ["version-update:semver-major"]
      - dependency-name: "three"
        update-types: ["version-update:semver-major", "version-update:semver-minor"]

  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    labels:
      - dependencies
